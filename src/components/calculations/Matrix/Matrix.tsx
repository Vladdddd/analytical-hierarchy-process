import { Box } from "@mui/material";
import { useFormStore } from "@/store/useFormStore";

export const Matrix = () => {
  const { alternatives, criteria } = useFormStore();

  return <Box>Matrix</Box>;
};
